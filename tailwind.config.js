module.exports = {
  future: {
    // removeDeprecatedGapUtilities: true,
    // purgeLayersByDefault: true,
  },
  purge: [],
  theme: {
    extend: {
      colors: {
        half: 'rgba(0, 0, 0, .24)',
        dark: 'rgba(0, 0, 0, .45)'
      },
      fontFamily: {
        nunito: ['nunito']
      }
    },
  },
  variants: {},
  plugins: [],
}
